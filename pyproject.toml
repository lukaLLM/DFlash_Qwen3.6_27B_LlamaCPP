[project]
name = "dflash-benchmark"
version = "0.1.0"
description = "Side-by-side speed comparison: Qwen 3.6 baseline vs Qwen 3.6 + DFlash speculative decoding."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hf-transfer>=0.1.9",
    "huggingface-hub>=1.23.0",
]
